# packages/scrapers

This package will contain:

- source adapters
- fetch utilities
- parser fixtures
- normalization entrypoints
- CLI scripts for manual and scheduled ingestion

Source adapters should stay isolated so one broken portal does not contaminate the rest of the pipeline.
