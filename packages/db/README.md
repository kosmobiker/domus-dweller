# packages/db

This package will contain:

- schema definitions
- migrations
- seed/reference data
- query helpers
- aggregation SQL

The database model should preserve historical observations and expose fast read paths for the web app.
