# data

Local storage for ingestion outputs.

Expected artifacts:

- DuckDB file (for example `warehouse.duckdb`)
- exported snapshots such as `current_listings.csv` and `current_listings.json`

This directory is gitignored except for this file and `.gitkeep`.
