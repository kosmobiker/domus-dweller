domus_dweller:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DEV_DB', 'data/dev.duckdb') }}"
      threads: 4
    prod:
      type: duckdb
      path: "md:{{ env_var('MD_DATABASE', 'my_db') }}?token={{ env_var('MOTHERDUCK_TOKEN') }}"
      threads: 4
