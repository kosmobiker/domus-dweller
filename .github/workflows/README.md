# Workflows

GitHub Actions is intended to handle:

- scheduled source ingestion
- daily aggregations
- smoke tests

The actual workflow files should be added only after the first source adapter and database schema exist, so the CI pipeline reflects real commands instead of placeholders.
